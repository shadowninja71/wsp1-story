{% block content %}
<h1>{{ page.titel }}</h1>
<p>{{ page.description }}</p>
{% for choice in page.choices %}
    <p>{{ choice.text }}</p>
    <p><a href="/story/{{ choice.target_id }}">{{ choice.target_id }}</a></p>
{% endfor %}
{% endblock %}